import React, { FC } from 'react';
import {
  StyleSheet,  
  Image
} from 'react-native';
import { MediaType } from '../services/models';


const CustomXXX: FC<MediaType>=(props) => {
  const { type, src } = props  

  return (
    <>
      
    </>
    
  );
};

const styles = StyleSheet.create({
  
});

export default CustomXXX;