import React, { FC } from 'react';
import {
  StyleSheet,  
  Image
} from 'react-native';
import { PicType } from '../services/models';


const CustomXXX: FC<PicType>=(props) => {
  const { type, src } = props  

  return (
    <>
      
    </>
    
  );
};

const styles = StyleSheet.create({
  
});

export default CustomXXX;